export class User {

  constructor(public login:string, public avatar_url:string, public location:string, public public_gists:number, public public_repos:number, public name: string, public company: string, public email: string, public hireable: boolean,public bio: string, public twitter_username: string, public followers: number, public following: number, public created_at: Date, public html_url: string){}
}
